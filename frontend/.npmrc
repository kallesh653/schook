# NPM Configuration for School Management System
# This file ensures smooth installation without --legacy-peer-deps flag

# Automatically resolve peer dependency conflicts
legacy-peer-deps=true

# Use exact versions for production stability
save-exact=false

# Faster installation
prefer-offline=true

# Engine strict mode (optional - can disable if needed)
engine-strict=false
